---
phase: 03-audit-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/core/audit.js, tests/core/audit.test.js]
autonomous: true
requirements: [AUDIT-01, AUDIT-02]
must_haves:
  truths:
    - "Audit engine identifies NOASSERTION fields in names, versions, and licenses."
    - "Audit engine identifies calculated hashes requiring manual verification."
    - "Audit engine identifies missing mandatory BSI properties (e.g., filename)."
  artifacts:
    - path: "src/core/audit.js"
      provides: "Modular rules engine for compliance auditing"
  key_links:
    - from: "src/core/audit.js"
      to: "individual rules"
      via: "functional array of rules"
---

<objective>
Implement a modular audit engine and the initial set of compliance rules to identify gaps in generated SBOMs according to BSI TR-03183-2.

Purpose: Automate the identification of "incomplete" data that requires manual officer review.
Output: A functional audit engine capable of flagging structural and compliance gaps.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/3-CONTEXT.md
@.planning/codebase/3-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Audit Engine Core and Basic Rules</name>
  <files>src/core/audit.js, tests/core/audit.test.js</files>
  <behavior>
    - Test 1: runAudit returns empty list for perfect SBOM.
    - Test 2: runAudit flags NOASSERTION in component name, version, or license.
    - Test 3: runAudit flags LicenseRef-ROWE-Unknown as an audit finding.
  </behavior>
  <action>
    Create src/core/audit.js. 
    Define an array of rule objects following the pattern from 3-RESEARCH.md.
    Implement 'completeness' rule to scan components and metadata for NOASSERTION and unknown license refs.
    Export runAudit(sbom) function that iterates over rules and aggregates findings.
  </action>
  <verify>
    <automated>npm test tests/core/audit.test.js</automated>
  </verify>
  <done>Audit engine exists and correctly flags completeness issues.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Integrity and BSI Property Rules</name>
  <files>src/core/audit.js</files>
  <behavior>
    - Test 1: Flag component if 'bsi:hash-source' is 'calculated'.
    - Test 2: Flag composition if 'aggregate' is 'incomplete'.
    - Test 3: Flag component if mandatory BSI properties like 'bsi:component:filename' are missing.
  </behavior>
  <action>
    Add 'integrity' rule to flag fields requiring manual verification (calculated hashes).
    Add 'compliance' rule to check for BSI-specific mandatory properties and composition status.
    Ensure findings include stable location identifiers using the pattern: "Component: [name]@[version] (PURL: [purl])".
  </action>
  <verify>
    <automated>npm test tests/core/audit.test.js</automated>
  </verify>
  <done>Audit engine covers hash integrity, aggregate status, and BSI property gaps with stable identifiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SBOM JSON → Engine | Untrusted JSON input from file system |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Audit Findings | mitigate | Findings are stored in a new array, no modification of input SBOM. |
| T-03-01-02 | Information Disclosure | Findings Location | mitigate | Ensure location strings use public metadata (purl/name), no internal file paths. |
</threat_model>

<verification>
Run `npm test tests/core/audit.test.js` to ensure all 6 core audit scenarios (completeness, integrity, compliance) are handled.
</verification>

<success_criteria>
- Audit engine detects 100% of targeted issues in a sample "dirty" SBOM.
- Findings follow the structured format (severity, message, location, remediation).
- Engine is extensible for future rules.
</success_criteria>

<output>
After completion, create `.planning/phases/03-audit-workflow/03-01-SUMMARY.md`
</output>

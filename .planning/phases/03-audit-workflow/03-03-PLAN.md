---
phase: 03-audit-workflow
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified: [src/commands/audit.js, bin/sbom-tool.mjs, tests/commands/audit.test.js]
autonomous: true
requirements: [CLI-03]
must_haves:
  truths:
    - "User can run 'sbom-tool audit <file>' and receive reports."
    - "Audit command fails gracefully if input file is missing or invalid JSON."
    - "Audit command creates output directory if it does not exist."
    - "Validation logic is executed as a preliminary step."
  artifacts:
    - path: "src/commands/audit.js"
      provides: "CLI action for SBOM auditing"
  key_links:
    - from: "src/commands/audit.js"
      to: "src/core/validation.js"
      via: "import and execution"
    - from: "src/commands/audit.js"
      to: "src/core/audit.js"
      via: "import and execution"
---

<objective>
Implement the `audit` CLI command and integrate the audit engine and report generators into the main tool.

Purpose: Provide the end-user interface for the audit workflow.
Output: A functional `audit` command in the `sbom-tool` CLI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/codebase/3-CONTEXT.md
@.planning/phases/03-audit-workflow/03-01-SUMMARY.md
@.planning/phases/03-audit-workflow/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Audit CLI Command</name>
  <files>src/commands/audit.js, bin/sbom-tool.mjs, tests/commands/audit.test.js</files>
  <behavior>
    - Test 1: Command accepts input file and options (--output-dir, --format).
    - Test 2: Command creates output directory if it doesn't exist.
    - Test 3: Command writes both .md and .json files by default.
  </behavior>
  <action>
    Create src/commands/audit.js.
    Define the audit action using commander.js logic.
    Register the 'audit' command in bin/sbom-tool.mjs.
    Ensure it handles file reading and directory creation (fs.mkdirSync recursive).
  </action>
  <verify>
    <automated>npm test tests/commands/audit.test.js</automated>
  </verify>
  <done>Audit command is registered and handles basic I/O.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Validation and Audit Engine</name>
  <files>src/commands/audit.js</files>
  <behavior>
    - Test 1: Validation errors from src/core/validation.js are included as 'error' findings.
    - Test 2: Audit findings from src/core/audit.js are included.
    - Test 3: Reports are generated only if audit process completes.
  </behavior>
  <action>
    Modify audit action to first call validateSbom from src/core/validation.js.
    Map validation errors to the finding format.
    Then call runAudit from src/core/audit.js.
    Combine findings and pass them to report generators.
  </action>
  <verify>
    <automated>npm test tests/commands/audit.test.js</automated>
  </verify>
  <done>Audit command performs full validation + compliance check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI Args → Command | Untrusted input from user command line |
| File System → Command | Reading arbitrary JSON files |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | Path Traversal | mitigate | Use path.resolve and path.basename to ensure output files are written to the intended directory only. |
| T-03-03-02 | Denial of Service | Malformed JSON | mitigate | Wrap JSON.parse in try-catch and report as a user error, not a crash. |
</threat_model>

<verification>
Run `node bin/sbom-tool.mjs audit project-bom.json --output-dir ./test-audit/` and verify the contents of the generated reports.
</verification>

<success_criteria>
- The tool identifies both structural (schema) and compliance (audit) issues in one pass.
- Output directory and file naming are correct.
- Exit codes are appropriate (e.g., 0 if audit ran, non-zero if fatal I/O error).
</success_criteria>

<output>
After completion, create `.planning/phases/03-audit-workflow/03-03-SUMMARY.md`
</output>

---
phase: 03-audit-workflow
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/utils/reports.js, tests/utils/reports.test.js]
autonomous: true
requirements: [REPORT-01, REPORT-02]
must_haves:
  truths:
    - "Generated Markdown report contains a summary table of findings."
    - "Generated JSON report contains all findings in machine-readable format."
    - "Reports include remediation advice for each finding."
  artifacts:
    - path: "src/utils/reports.js"
      provides: "Template-based Markdown and JSON report generators"
---

<objective>
Implement report generators to transform audit findings into human-readable (Markdown) and machine-readable (JSON) formats.

Purpose: Provide actionable feedback to compliance officers and CI/CD pipelines.
Output: Utilities for generating standardized audit reports.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/codebase/3-RESEARCH.md
@.planning/phases/03-audit-workflow/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Markdown Report Generator</name>
  <files>src/utils/reports.js, tests/utils/reports.test.js</files>
  <behavior>
    - Test 1: generateMarkdownReport includes 'SBOM Audit Report' header.
    - Test 2: summary section reflects correct counts of errors/warnings.
    - Test 3: findings table includes severity, rule, and message.
  </behavior>
  <action>
    Create src/utils/reports.js.
    Implement generateMarkdownReport(findings, metadata) using ES6 template literals.
    Follow the layout suggested in 3-RESEARCH.md (Summary, Findings Table, Action Items).
  </action>
  <verify>
    <automated>npm test tests/utils/reports.test.js</automated>
  </verify>
  <done>Markdown report generator produces compliant and readable output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement JSON Report Generator</name>
  <files>src/utils/reports.js</files>
  <behavior>
    - Test 1: generateJSONReport returns a valid JSON string.
    - Test 2: output contains metadata (timestamp, target) and findings array.
  </behavior>
  <action>
    Implement generateJSONReport(findings, metadata) in src/utils/reports.js.
    Simply use JSON.stringify with proper indentation.
  </action>
  <verify>
    <automated>npm test tests/utils/reports.test.js</automated>
  </verify>
  <done>JSON report generator produces valid machine-readable findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Findings Array → Reports | Internal data being formatted for output |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | Reports | mitigate | Ensure no sensitive environment variables or local paths are included in the metadata. |
</threat_model>

<verification>
Run `npm test tests/utils/reports.test.js` to verify both formats generate correctly given a sample findings array.
</verification>

<success_criteria>
- Reports accurately reflect the input findings.
- Markdown is valid and renders correctly in standard viewers.
- JSON is parseable and follows a consistent schema.
</success_criteria>

<output>
After completion, create `.planning/phases/03-audit-workflow/03-02-SUMMARY.md`
</output>

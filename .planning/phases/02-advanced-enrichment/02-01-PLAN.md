---
phase: 02-advanced-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/utils/preflight.js, src/utils/hash.js, src/utils/config.js, tests/utils/preflight.test.js, tests/utils/hash.test.js, tests/utils/config.test.js]
autonomous: true
requirements: [ENR-01, ORCH-01, CONF-02]
must_haves:
  truths:
    - "Pre-flight checks can detect cdxgen existence and version"
    - "cdxgen can be installed locally without global flags if missing"
    - "Local SHA-512 calculation matches standard tool output"
    - "Configuration values can be overridden by environment variables"
  artifacts:
    - path: "src/utils/preflight.js"
      provides: "cdxgen availability and local installation"
    - path: "src/utils/hash.js"
      provides: "SHA-512 calculation for local files"
    - path: "src/utils/config.js"
      provides: "Env fallback for sbom.config.json"
  key_links:
    - from: "src/utils/config.js"
      to: "process.env"
      via: "fallback mechanism"
---

<objective>
Establish the foundation for pre-flight checks, local tool management, hash calculation, and configuration environment fallbacks.

Purpose: Robust environment management and foundational utilities for the enrichment loop.
Output: New utilities for tool management and hashing, updated configuration module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/2-CONTEXT.md
@.planning/codebase/2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight & Command Management</name>
  <files>package.json, src/utils/preflight.js, tests/utils/preflight.test.js</files>
  <action>
    - Add `which` and `axios` to `package.json` dependencies.
    - Implement `src/utils/preflight.js` with `checkCdxgen()` and `installCdxgen()` functions.
    - Use `which` for robust cross-platform detection.
    - Implement installation logic that uses `npm install --prefix ./tools @cyclonedx/cdxgen`.
    - DO NOT use the `-g` (global) flag for installation (per D-03/checker feedback).
    - Ensure `checkCdxgen()` looks for the local bin at `./tools/node_modules/.bin/cdxgen` if not found in system PATH.
    - Add unit tests in `tests/utils/preflight.test.js`.
  </action>
  <verify>
    <automated>npm test tests/utils/preflight.test.js</automated>
  </verify>
  <done>cdxgen detection and local installation logic is implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Local Hash Tier Utility</name>
  <files>src/utils/hash.js, tests/utils/hash.test.js</files>
  <action>
    - Implement `src/utils/hash.js` exporting `calculateSha512(filePath)`.
    - Use Node.js built-in `crypto` module.
    - Return lowercase hex string.
    - Add unit tests in `tests/utils/hash.test.js` verifying against known file hashes.
  </action>
  <verify>
    <automated>npm test tests/utils/hash.test.js</automated>
  </verify>
  <done>SHA-512 utility is implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Config Environment Support</name>
  <files>src/utils/config.js, tests/utils/config.test.js</files>
  <action>
    - Update `src/utils/config.js` to support environment variable fallbacks (e.g., `process.env.SBOM_CREATOR_EMAIL || config.creatorEmail`).
    - Map all core config fields to standard ENV names:
      - `creatorEmail` -> `SBOM_CREATOR_EMAIL`
      - `creatorUrl` -> `SBOM_CREATOR_URL`
      - `specVersion` -> `SBOM_SPEC_VERSION`
      - `cdxgenVersion` -> `CDXGEN_VERSION`
    - Add unit tests in `tests/utils/config.test.js`.
  </action>
  <verify>
    <automated>npm test tests/utils/config.test.js</automated>
  </verify>
  <done>Configuration module supports environment variable overrides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tooling → Environment | cdxgen installation interacts with local ./tools directory. |
| Configuration → Tool | Env vars could be used to inject malicious tool paths or versions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Preflight / PATH | mitigate | Prioritize local ./tools installation path over system PATH to avoid hijacking. Use absolute paths where possible. |
| T-02-01-02 | Information Disclosure | Config / Env | mitigate | Do not log sensitive env vars in CLI output unless explicitly requested. |
</threat_model>

<verification>
Ensure all unit tests pass and `npm install` works with new dependencies.
</verification>

<success_criteria>
- `preflight.js` correctly identifies if `cdxgen` is available.
- `cdxgen` is installed to `./tools` if missing from PATH.
- `hash.js` produces valid SHA-512 hashes.
- `config.js` prioritizes ENV vars over JSON file.
</success_criteria>

<output>
After completion, create `.planning/phases/02-advanced-enrichment/02-01-SUMMARY.md`
</output>

---
phase: 02-advanced-enrichment
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified: [src/commands/generate.js]
autonomous: false
requirements: [ORCH-01, ORCH-02]
must_haves:
  truths:
    - "CLI performs pre-flight checks before starting SBOM generation"
    - "CLI uses isolated workspace for generation and cleans up on completion"
    - "Final SBOM passes double validation (Schema + Dynamic BSI Rules)"
  artifacts:
    - path: "src/commands/generate.js"
      provides: "Integrated CLI workflow with pre-flight and isolation"
  key_links:
    - from: "src/commands/generate.js"
      to: "src/utils/preflight.js"
      via: "Tool check"
    - from: "src/commands/generate.js"
      to: "src/utils/isolation.js"
      via: "Workspace setup"
---

<objective>
Integrate all utilities and logic into the master CLI generator workflow and perform final phase verification.

Purpose: Delivering a fully automated, high-integrity SBOM generator.
Output: Refactored generate command, verified Phase 2 outcome.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/2-CONTEXT.md
@.planning/codebase/2-RESEARCH.md
@.planning/phases/02-advanced-enrichment/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI Master Orchestration</name>
  <files>src/commands/generate.js</files>
  <action>
    - Update `src/commands/generate.js` to integrate all Phase 2 features.
    - 1. Pre-flight: Call `checkCdxgen()` and if missing, call `installCdxgen()`.
    - 2. Isolation: Call `prepareIsolation(projectPath)` to create a temporary manifest-only workspace.
    - 3. Generation: Run `cdxgen` (using detected path from pre-flight) within the isolated workspace.
    - 4. Enrichment: Apply the refactored tiered enrichment loop from `src/core/enrichment.js`.
    - 5. Validation: Run full validation using `src/core/validation.js` (Schema + Dynamic BSI Rules).
    - 6. Cleanup: Ensure temporary isolation directories are removed regardless of success/failure.
    - Ensure meaningful error messages at each stage.
  </action>
  <verify>
    <automated>bin/sbom-tool.mjs generate . --output test-bom.json</automated>
  </verify>
  <done>CLI generate command is fully integrated with pre-flight, isolation, tiered enrichment, and dynamic validation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Final Phase 2 Verification</name>
  <what-built>Phase 2: Master Orchestrator & Advanced Enrichment</what-built>
  <how-to-verify>
    1. Clean any existing `tools/` or global `cdxgen` to test pre-flight.
    2. Run `bin/sbom-tool.mjs generate . --output project-bom.json`.
    3. Verify that `tools/` is created and `cdxgen` is installed if it wasn't there.
    4. Inspect `project-bom.json`:
       - Verify external dependencies have SHA-512 and authoritative licenses.
       - Verify local components have `bsi:hash-source: calculated`.
       - Verify properties match official BSI taxonomy.
    5. Run `bin/sbom-tool.mjs validate project-bom.json`.
       - Ensure it passes both schema and BSI rules.
    6. Verify that no temporary isolation directories were left behind.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
  <done>Phase 2 is verified and ready for completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool → Environment | Generation process touches local filesystem and network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Final SBOM | mitigate | Final strict validation (Schema + Dynamic BSI Rules) before writing output. |
| T-02-04-02 | Resource Exhaustion | Cleanup | mitigate | Use `try...finally` or `process.on('exit')` to guarantee isolation cleanup. |
</threat_model>

<verification>
Full end-to-end verification of the generation flow.
</verification>

<success_criteria>
- SBOM generation completes without errors on multiple project types.
- Pre-flight handles missing `cdxgen` gracefully.
- Isolation logic prevents project contamination.
- Final output is BSI-compliant.
</success_criteria>

<output>
After completion, create `.planning/phases/02-advanced-enrichment/02-04-SUMMARY.md`
</output>

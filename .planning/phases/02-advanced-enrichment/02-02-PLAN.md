---
phase: 02-advanced-enrichment
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/utils/taxonomy-parser.js, src/core/enrichment/npm-provider.js, src/core/enrichment/local-provider.js, src/core/enrichment/git-provider.js, tests/utils/taxonomy-parser.test.js, tests/core/enrichment/providers.test.js]
autonomous: true
requirements: [ENR-01, TAXO-02]
must_haves:
  truths:
    - "Dynamic parser extracts bsi:component properties from BSI README"
    - "NPM provider fetches authoritative SHA-512 from Registry"
    - "Local provider reads license from node_modules"
    - "Git provider extracts browse URL for Git repositories"
  artifacts:
    - path: "src/utils/taxonomy-parser.js"
      provides: "BSI taxonomy extraction"
    - path: "src/core/enrichment/npm-provider.js"
      provides: "Tier 1: Registry data"
    - path: "src/core/enrichment/local-provider.js"
      provides: "Tier 2: Local data"
    - path: "src/core/enrichment/git-provider.js"
      provides: "Tier 3: Git source URIs"
  key_links:
    - from: "src/utils/taxonomy-parser.js"
      to: "GitHub"
      via: "HTTP fetch"
      pattern: "fetch.*raw.githubusercontent.com"
---

<objective>
Implement the dynamic taxonomy parser and individual enrichment providers (Tiers 1-3).

Purpose: Enabling external data sourcing and dynamic rule enforcement.
Output: Dynamic parser for BSI taxonomy, specialized metadata providers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/2-CONTEXT.md
@.planning/codebase/2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic BSI Taxonomy Parser</name>
  <files>src/utils/taxonomy-parser.js, tests/utils/taxonomy-parser.test.js</files>
  <action>
    - Create `src/utils/taxonomy-parser.js`.
    - Implement `fetchTaxonomy()` to get Markdown from `https://raw.githubusercontent.com/BSI-Bund/tr-03183-cyclonedx-property-taxonomy/main/README.md`.
    - Implement `parseTaxonomy(markdown)` using regex: `/\|\s*\*\*`(bsi:[a-z:]+)`\*\*\s*\|/gi`.
    - Implement caching to local file (e.g., `schemas/bsi-taxonomy.cache.json`) for offline fallback.
    - Export `getAllowedProperties()`.
    - Add unit tests in `tests/utils/taxonomy-parser.test.js`.
  </action>
  <verify>
    <automated>npm test tests/utils/taxonomy-parser.test.js</automated>
  </verify>
  <done>BSI taxonomy can be fetched and parsed dynamically.</done>
</task>

<task type="auto">
  <name>Task 2: NPM Registry Provider (Tier 1)</name>
  <files>src/core/enrichment/npm-provider.js, tests/core/enrichment/providers.test.js</files>
  <action>
    - Create `src/core/enrichment/npm-provider.js`.
    - Implement `fetchRegistryData(name, version)` using `https://registry.npmjs.org/[name]/[version]`.
    - Extract `dist.integrity` (SHA-512 preferred) and license string/object.
    - Add unit tests (mock registry responses) in `tests/core/enrichment/providers.test.js`.
  </action>
  <verify>
    <automated>npm test tests/core/enrichment/providers.test.js</automated>
  </verify>
  <done>NPM Registry data provider is implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Local & Git Providers (Tiers 2-3)</name>
  <files>src/core/enrichment/local-provider.js, src/core/enrichment/git-provider.js, tests/core/enrichment/providers.test.js</files>
  <action>
    - Implement `src/core/enrichment/local-provider.js` to read from `node_modules/[pkg]/package.json`.
    - Implement `src/core/enrichment/git-provider.js` using `hosted-git-info` to normalize Git URIs to browseable URLs.
    - Add unit tests in `tests/core/enrichment/providers.test.js`.
  </action>
  <verify>
    <automated>npm test tests/core/enrichment/providers.test.js</automated>
  </verify>
  <done>Local and Git metadata providers are implemented and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool → Remote Registry | Fetching from npm registry. |
| Tool → Remote GitHub | Fetching taxonomy from GitHub. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | Remote Data | mitigate | Validate fetched data structure (JSON/Markdown) before parsing. Use offline cache if remote data is malformed. |
| T-02-02-02 | Denial of Service | NPM Registry | mitigate | Implement basic retry logic with backoff in axios for 429 errors. |
</threat_model>

<verification>
Unit tests pass for all providers and taxonomy parser.
</verification>

<success_criteria>
- `taxonomy-parser` successfully parses the official BSI README.
- `npm-provider` retrieves valid integrity and license data.
- `git-provider` correctly handles various Git URL formats.
</success_criteria>

<output>
After completion, create `.planning/phases/02-advanced-enrichment/02-02-SUMMARY.md`
</output>

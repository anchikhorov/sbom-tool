---
phase: 02-advanced-enrichment
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/core/enrichment.js, src/core/validation.js, src/utils/isolation.js, tests/core/enrichment.test.js, tests/core/validation.test.js, tests/utils/isolation.test.js]
autonomous: true
requirements: [ENR-01, VAL-02, ORCH-02]
must_haves:
  truths:
    - "Master enrichment loop correctly attempts Tier 1 -> Tier 2 -> Tier 3 -> Tier 4"
    - "Calculated SHA-512 is tagged with bsi:hash-source: calculated"
    - "Validation dynamically checks bsi properties against fetched taxonomy"
    - "Isolation logic can create a clean workspace for NPM, Maven, and Python projects"
  artifacts:
    - path: "src/core/enrichment.js"
      provides: "Tiered enrichment loop implementation"
    - path: "src/core/validation.js"
      provides: "Dynamic BSI rule validation"
    - path: "src/utils/isolation.js"
      provides: "Environment isolation for cdxgen"
  key_links:
    - from: "src/core/enrichment.js"
      to: "src/core/enrichment/npm-provider.js"
      via: "Tier 1 lookup"
    - from: "src/core/validation.js"
      to: "src/utils/taxonomy-parser.js"
      via: "dynamic rule lookup"
---

<objective>
Implement the tiered enrichment loop, dynamic validation, and environment isolation logic.

Purpose: Completing the core generation logic with high integrity and environment awareness.
Output: Refactored enrichment and validation modules, new isolation utility.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/2-CONTEXT.md
@.planning/codebase/2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Master Enrichment Loop</name>
  <files>src/core/enrichment.js, tests/core/enrichment.test.js</files>
  <action>
    - Refactor `enrichComponent` in `src/core/enrichment.js` to use the tiered strategy.
    - Tier 1: `npm-provider.js` (for npm packages).
    - Tier 2: `local-provider.js` (if Tier 1 fails or for other types).
    - Tier 3: `git-provider.js` (extract browse URL).
    - Tier 4: `hash.js` (calculate SHA-512 if missing).
    - Ensure Tier 4 hash is accompanied by property `bsi:hash-source: calculated`.
    - Improve license handling: fallback to `LicenseRef-ROWE-Unknown` only if all tiers fail to find a license.
    - Update unit tests in `tests/core/enrichment.test.js`.
  </action>
  <verify>
    <automated>npm test tests/core/enrichment.test.js</automated>
  </verify>
  <done>Tiered enrichment loop is implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic Validation Integration</name>
  <files>src/core/validation.js, tests/core/validation.test.js</files>
  <action>
    - Update `src/core/validation.js` to use `taxonomy-parser.js`.
    - Implement a dynamic check for `bsi:*` properties: only allow properties present in the taxonomy.
    - Add unit tests for dynamic validation.
  </action>
  <verify>
    <automated>npm test tests/core/validation.test.js</automated>
  </verify>
  <done>Validation now uses dynamic taxonomy rules.</done>
</task>

<task type="auto">
  <name>Task 3: Environment Isolation Logic</name>
  <files>src/utils/isolation.js, tests/utils/isolation.test.js</files>
  <action>
    - Create `src/utils/isolation.js`.
    - Implement logic to handle diverse project types (NPM, Maven, Python) per D-03.
    - Logic should:
      1. Detect project type by manifest files (`package.json`, `pom.xml`, `requirements.txt`).
      2. Create a temporary directory.
      3. Copy ONLY the relevant manifest files to the temporary directory to isolate the environment for `cdxgen`.
      4. Export a function `prepareIsolation(projectPath)` that returns the temporary directory path and project type.
    - Add unit tests in `tests/utils/isolation.test.js`.
  </action>
  <verify>
    <automated>npm test tests/utils/isolation.test.js</automated>
  </verify>
  <done>Environment isolation logic is implemented and tested for NPM, Maven, and Python.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool → Filesystem | Isolation logic creates temporary directories and copies files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | Isolation / Temp Dir | mitigate | Ensure temporary directories are created with restricted permissions (e.g., 0700) and cleaned up. |
| T-02-03-02 | Denial of Service | Isolation / Disk Space | mitigate | Limit the size/number of files copied to isolation workspace to prevent disk exhaustion. |
</threat_model>

<verification>
Verify that enrichment, validation, and isolation unit tests all pass.
</verification>

<success_criteria>
- Enrichment loop follows the 4-tier strategy correctly.
- Dynamic validation correctly rejects properties not in the BSI taxonomy.
- Isolation logic correctly identifies and prepares workspaces for NPM, Maven, and Python projects.
</success_criteria>

<output>
After completion, create `.planning/phases/02-advanced-enrichment/02-03-SUMMARY.md`
</output>

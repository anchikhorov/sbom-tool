---
phase: 02-advanced-enrichment
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/core/enrichment.js, src/core/validation.js, src/commands/generate.js, tests/core/enrichment.test.js, tests/core/validation.test.js]
autonomous: false
requirements: [ENR-01, VAL-02, ORCH-02]
must_haves:
  truths:
    - "Master enrichment loop correctly attempts Tier 1 -> Tier 2 -> Tier 3 -> Tier 4"
    - "Calculated SHA-512 is tagged with bsi:hash-source: calculated"
    - "Validation dynamically checks bsi properties against fetched taxonomy"
    - "CLI perform pre-flight checks before starting SBOM generation"
  artifacts:
    - path: "src/core/enrichment.js"
      provides: "Tiered enrichment loop implementation"
    - path: "src/core/validation.js"
      provides: "Dynamic BSI rule validation"
    - path: "src/commands/generate.js"
      provides: "Integrated CLI workflow with pre-flight"
  key_links:
    - from: "src/core/enrichment.js"
      to: "src/core/enrichment/npm-provider.js"
      via: "Tier 1 lookup"
    - from: "src/core/validation.js"
      to: "src/utils/taxonomy-parser.js"
      via: "dynamic rule lookup"
---

<objective>
Integrate all providers into the master enrichment loop and update validation to use dynamic taxonomy rules.

Purpose: Completing the high-integrity SBOM generation flow.
Output: Refactored enrichment and validation modules, integrated CLI workflow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/2-CONTEXT.md
@.planning/codebase/2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Master Enrichment Loop</name>
  <files>src/core/enrichment.js, tests/core/enrichment.test.js</files>
  <action>
    - Refactor `enrichComponent` in `src/core/enrichment.js` to use the tiered strategy.
    - Tier 1: `npm-provider.js` (for npm packages).
    - Tier 2: `local-provider.js` (if Tier 1 fails or for other types).
    - Tier 3: `git-provider.js` (extract browse URL).
    - Tier 4: `hash.js` (calculate SHA-512 if missing).
    - Ensure Tier 4 hash is accompanied by property `bsi:hash-source: calculated`.
    - Improve license handling: fallback to `LicenseRef-ROWE-Unknown` only if all tiers fail to find a license.
    - Update unit tests in `tests/core/enrichment.test.js`.
  </action>
  <verify>
    <automated>npm test tests/core/enrichment.test.js</automated>
  </verify>
  <done>Tiered enrichment loop is implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic Validation Integration</name>
  <files>src/core/validation.js, tests/core/validation.test.js</files>
  <action>
    - Update `src/core/validation.js` to use `taxonomy-parser.js`.
    - Implement a dynamic check for `bsi:*` properties: only allow properties present in the taxonomy.
    - Add unit tests for dynamic validation.
  </action>
  <verify>
    <automated>npm test tests/core/validation.test.js</automated>
  </verify>
  <done>Validation now uses dynamic taxonomy rules.</done>
</task>

<task type="auto">
  <name>Task 3: CLI Integration & Verification</name>
  <files>src/commands/generate.js</files>
  <action>
    - Update `src/commands/generate.js`.
    - Use `src/utils/preflight.js` to ensure `cdxgen` is available before processing.
    - Integrate the refactored enrichment logic.
    - Add a final validation step using `src/core/validation.js` (both schema and dynamic BSI rules).
    - Ensure it cleans up isolated environment on failure.
  </action>
  <verify>
    <automated>bin/sbom-tool.mjs generate . --output test-bom.json</automated>
  </verify>
  <done>CLI workflow is updated and verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 2: Master Orchestrator & Advanced Enrichment</what-built>
  <how-to-verify>
    1. Run `bin/sbom-tool.mjs generate . --output project-bom.json`.
    2. Inspect `project-bom.json`:
       - Verify external dependencies have SHA-512 and authoritative licenses.
       - Verify local components have `bsi:hash-source: calculated`.
       - Verify properties match official BSI taxonomy.
    3. Run `bin/sbom-tool.mjs validate project-bom.json`.
       - Ensure it passes both schema and BSI rules.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool → Remote Systems | The enrichment loop and validation pull from multiple remote sources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Spoofing | Registry Data | mitigate | Cross-check NPM registry data with Tier 2 (Local Disk) where possible to detect integrity mismatches. |
| T-02-03-02 | Tampering | Final SBOM | mitigate | Perform strict validation (Schema + BSI Rules) as the final step before writing the output file. |
</threat_model>

<verification>
Verify full end-to-end flow with a sample project.
</verification>

<success_criteria>
- SBOM generation completes without errors.
- External packages are enriched with Registry data.
- BSI properties are valid according to dynamic taxonomy.
- `cdxgen` is automatically handled if missing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-advanced-enrichment/02-03-SUMMARY.md`
</output>

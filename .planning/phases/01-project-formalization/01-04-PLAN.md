---
phase: 01-project-formalization
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified: [bin/sbom-tool.mjs, src/commands/generate.js, src/commands/validate.js, src/utils/config.js, tests/cli.test.js]
autonomous: true
requirements: [CLI-01, CONF-01, TEST-01]
must_haves:
  truths:
    - "sbom-tool --help displays all commands"
    - "generate command produces a valid, enriched SBOM file"
    - "validate command correctly reports compliance status"
  artifacts:
    - path: "bin/sbom-tool.mjs"
      provides: "CLI entry point"
    - path: "src/utils/config.js"
      provides: "Configuration discovery logic"
  key_links:
    - from: "bin/sbom-tool.mjs"
      to: "commander"
      via: "command registration"
      pattern: "new Command\\(\\)"
---

<objective>
Implement the CLI interface using commander.js and support `sbom.config.json` auto-discovery.

Purpose: Provide a professional CLI tool and flexible configuration.
Output: Working CLI entry point with `generate` and `validate` commands.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/1-RESEARCH.md
@generate-sbom.mjs
@validate-sbom.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Configuration Discovery</name>
  <files>src/utils/config.js</files>
  <action>
    - Create logic to find and load `sbom.config.json` in the current working directory.
    - Support environment variables fallback: `SBOM_CREATOR_EMAIL`, `SBOM_CREATOR_URL`.
    - Provide defaults for enrichment (creator info).
  </action>
  <verify>
    <automated>node -e "import { loadConfig } from './src/utils/config.js'; console.log(loadConfig())"</automated>
  </verify>
  <done>Configuration discovery logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Build CLI Commands</name>
  <files>bin/sbom-tool.mjs, src/commands/generate.js, src/commands/validate.js, tests/cli.test.js</files>
  <action>
    - Use `commander.js` (per D-02).
    - Implement `generate <path>` command:
        - Port project isolation from `generate-sbom.mjs`.
        - Detect `cdxgen`.
        - Execute `cdxgen`.
        - Call `enrichSbom` (from 01-02).
        - Write output file.
    - Implement `validate <path>` command:
        - Load SBOM.
        - Call `validateSbom` (from 01-03).
        - Report errors.
    - Integration tests in `tests/cli.test.js`. **MUST use `vi.hoisted` patterns for mocking `node:child_process` and `node:fs` in ESM as identified in 1-RESEARCH.md.**
  </action>
  <verify>
    <automated>npx vitest tests/cli.test.js</automated>
  </verify>
  <done>CLI interface fully operational with mocked integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Command line input | Untrusted arguments |
| System execution | Shell execution of cdxgen |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | T | CLI input | mitigate | Sanitize paths and validate inputs with commander |
| T-01-04-02 | T | execSync | mitigate | Use spawn or sanitize inputs if shell: true is used |
</threat_model>

<verification>
Check `bin/sbom-tool.mjs --help`.
Run `generate` on a test folder.
Run `validate` on the output.
</verification>

<success_criteria>
- CLI with commander.js functional.
- Auto-discovery of `sbom.config.json` working.
- End-to-end generate and validate cycle successful.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-formalization/01-04-SUMMARY.md`
</output>

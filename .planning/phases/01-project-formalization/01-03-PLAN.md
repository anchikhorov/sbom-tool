---
phase: 01-project-formalization
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified: [schemas/bom-1.6.schema.json, src/core/validation.js, tests/core/validation.test.js]
autonomous: true
requirements: [VAL-01, TAXO-01, TEST-01]
must_haves:
  truths:
    - "CycloneDX 1.6 schema is available locally"
    - "Ajv validation correctly flags structural errors"
    - "taxonomy checks identify BSI-specific compliance gaps (e.g. missing SHA-512)"
  artifacts:
    - path: "schemas/bom-1.6.schema.json"
      provides: "CycloneDX 1.6 JSON schema"
    - path: "src/core/validation.js"
      provides: "SBOM validation logic"
  key_links:
    - from: "src/core/validation.js"
      to: "schemas/bom-1.6.schema.json"
      via: "import attributes"
      pattern: "with { type: \"json\" }"
---

<objective>
Bundle CycloneDX 1.6 schema and implement Ajv-based structural and taxonomy validation.

Purpose: Provide offline-capable, robust validation of generated SBOMs.
Output: Bundled schema, validation module, and unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/1-RESEARCH.md
@validate-sbom.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle CycloneDX Schema</name>
  <files>schemas/bom-1.6.schema.json</files>
  <action>
    - Download/copy the CycloneDX 1.6 JSON schema into `schemas/`.
    - Reference: https://cyclonedx.org/schema/bom-1.6.schema.json
  </action>
  <verify>
    <automated>ls schemas/bom-1.6.schema.json</automated>
  </verify>
  <done>Schema bundled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Validation Logic</name>
  <files>src/core/validation.js, tests/core/validation.test.js</files>
  <behavior>
    - Initialize Ajv with `ajv-formats`.
    - Implement `validateSbomStructure` using the bundled schema.
    - Port taxonomy checks from `validate-sbom.mjs`:
        - Global role check (supplier/manufacturer/authors).
        - Component SHA-512 check.
        - Component role check.
        - SPDX ID check (bundle common IDs or fetch once?).
        - BSI property check (filename, executable).
    - Use `Import Attributes` for loading schema in `src/core/validation.js`.
    - Test Case 1: Valid compliant SBOM.
    - Test Case 2: Missing SHA-512.
    - Test Case 3: Invalid SPDX ID.
  </behavior>
  <action>
    - Create `src/core/validation.js` exporting `validateSbom`.
    - Write unit tests in `tests/core/validation.test.js`.
  </action>
  <verify>
    <automated>npx vitest tests/core/validation.test.js</automated>
  </verify>
  <done>Validation logic implemented with Ajv and taxonomy checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Schema loading | Trusted local schema file |
| Input SBOM | Untrusted JSON input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | T | JSON input | mitigate | Use Ajv with strict schema validation |
</threat_model>

<verification>
`npx vitest tests/core/validation.test.js` passes.
</verification>

<success_criteria>
- CycloneDX 1.6 schema bundled.
- Validation logic correctly identifies both structural and BSI-specific issues.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-formalization/01-03-SUMMARY.md`
</output>

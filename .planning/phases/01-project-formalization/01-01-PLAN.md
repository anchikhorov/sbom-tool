---
phase: 01-project-formalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, .gitignore, vitest.config.mjs, src/index.js, bin/sbom-tool.mjs, schemas/.gitkeep, tests/.gitkeep]
autonomous: true
requirements: [FORM-01, STRUC-01, TEST-01]
must_haves:
  truths:
    - "package.json contains 'type': 'module'"
    - "vitest run successfully executes"
    - "standard directory structure exists (src/core, src/commands, bin, tests, schemas)"
  artifacts:
    - path: "package.json"
      provides: "Package definition and dependencies"
    - path: "vitest.config.mjs"
      provides: "Vitest configuration"
    - path: "bin/sbom-tool.mjs"
      provides: "CLI entry point"
  key_links:
    - from: "package.json"
      to: "vitest"
      via: "test script"
      pattern: "\"test\": \"vitest run\""
---

<objective>
Initialize the `@rowe/sbom-tool` as an ESM package and establish the foundational directory structure.

Purpose: Professional project formalization and setting up for modern Node.js development.
Output: Initialized package.json, directory structure, and testing environment.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/1-CONTEXT.md
@.planning/codebase/1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize ESM Package</name>
  <files>package.json, .gitignore</files>
  <action>
    - Create `package.json` for `@rowe/sbom-tool` (per D-06).
    - Set `"type": "module"` (per D-01).
    - Add scripts: `"test": "vitest run"`, `"dev": "vitest"`, `"lint": "eslint ."`.
    - Add dependencies from research: `commander`, `@cyclonedx/cdxgen`, `ajv`, `ajv-formats`.
    - Add dev dependencies: `vitest`, `memfs`.
    - Create `.gitignore` to exclude `node_modules`, `coverage`, and temporary outputs.
  </action>
  <verify>
    <automated>npm list --depth=0 && cat package.json | grep '"type": "module"'</automated>
  </verify>
  <done>Package initialized with correct type and dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Establish Directory Structure</name>
  <files>src/core/.gitkeep, src/commands/.gitkeep, bin/sbom-tool.mjs, tests/core/.gitkeep, tests/commands/.gitkeep, schemas/.gitkeep</files>
  <action>
    - Create the standard layout (per STRUCTURE.md and 1-CONTEXT.md):
      - `src/core/` for business logic.
      - `src/commands/` for CLI command handlers.
      - `bin/` for CLI entry point.
      - `tests/` for unit and integration tests.
      - `schemas/` for bundled JSON schemas.
    - Create a minimal `bin/sbom-tool.mjs` entry point that imports commander.
    - Add `.gitkeep` files to ensure directory persistence.
  </action>
  <verify>
    <automated>ls -R bin/ src/ tests/ schemas/</automated>
  </verify>
  <done>Standard directory structure established.</done>
</task>

<task type="auto">
  <name>Task 3: Configure Vitest and Skeletal Testing</name>
  <files>vitest.config.mjs, tests/sanity.test.js</files>
  <action>
    - Create `vitest.config.mjs` for testing (per D-05).
    - Ensure test scaffold supports `vi.hoisted` patterns for mocking ESM modules (e.g., `child_process`, `fs`) as identified in 1-RESEARCH.md.
    - Create a basic sanity test to ensure the environment is working.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Vitest configured and sanity test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| package setup | Trusted local setup |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | T | package.json | accept | Internal setup, minimal risk |
</threat_model>

<verification>
Ensure `package.json` exists, has `"type": "module"`, and `npm test` runs successfully.
</verification>

<success_criteria>
- Project initialized as ESM package.
- Directory structure exists.
- Vitest runs successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-formalization/01-01-SUMMARY.md`
</output>

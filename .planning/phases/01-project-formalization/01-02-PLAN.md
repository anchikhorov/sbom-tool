---
phase: 01-project-formalization
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/core/classification.js, src/core/enrichment.js, tests/core/classification.test.js, tests/core/enrichment.test.js]
autonomous: true
requirements: [CORE-01, TEST-01]
must_haves:
  truths:
    - "enrichSbom produces BSI-compliant objects with serialNumber and timestamp"
    - "classifyComponent correctly identifies executable/archive/structured types"
    - "unit tests for enrichment cover all BSI §5 fields including dependencies graph completeness"
  artifacts:
    - path: "src/core/classification.js"
      provides: "BSI classification logic"
    - path: "src/core/enrichment.js"
      provides: "BSI enrichment logic"
  key_links:
    - from: "src/core/enrichment.js"
      to: "BSI §5"
      via: "CycloneDX property mapping"
      pattern: "bsi:component:"
---

<objective>
Implement the core BSI enrichment and component classification logic into modular ESM files with unit tests.

Purpose: Isolate business logic from script-like execution for testability and reuse.
Output: `classification.js` and `enrichment.js` in `src/core/` with high test coverage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@generate-sbom.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Classification Logic</name>
  <files>src/core/classification.js, tests/core/classification.test.js</files>
  <behavior>
    - Port `classifyComponent` from `generate-sbom.mjs`.
    - Correctly identify:
        - `executable`: based on type, purl, or extensions (js, py, exe, etc).
        - `archive`: based on extensions (zip, tar, jar, etc) or type (container).
        - `structured`: based on extensions or type.
    - Test Case 1: NPM package (executable).
    - Test Case 2: Zip file (archive, structured).
    - Test Case 3: Library without extensions (not archive).
  </behavior>
  <action>
    - Create `src/core/classification.js` exporting `classifyComponent`.
    - Write unit tests in `tests/core/classification.test.js`.
  </action>
  <verify>
    <automated>npx vitest tests/core/classification.test.js</automated>
  </verify>
  <done>Classification logic correctly ports and passes tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Enrichment Logic</name>
  <files>src/core/enrichment.js, tests/core/enrichment.test.js</files>
  <behavior>
    - Port `enrichComponent` and main enrichment loop from `generate-sbom.mjs`.
    - Map BSI requirements (BSI TR-03183-2 §5) to CycloneDX fields:
        - metadata cleanup (tools, lifecycles, annotations).
        - manufacturer (creator email/URL fallback).
        - bsi properties (filename, executable, archive, structured, acknowledgement).
        - distribution URL derivation.
        - SHA-512 hash mapping.
        - **serialNumber** (SBOM-URI) derivation (§5.2.3).
        - **metadata.timestamp** (§5.2.1) enforcement.
        - **dependencies** graph completeness (ensuring all components have entries) (§5.2.2 + §5.1).
        - **compositions** aggregate status declaration (§5.2.2).
    - Test Case 1: Minimal SBOM enrichment.
    - Test Case 2: Component without bsi:filename.
    - Test Case 3: Component with existing SHA-512.
    - Test Case 4: SBOM missing serialNumber and timestamp.
    - Test Case 5: Component missing from dependencies graph.
  </behavior>
  <action>
    - Create `src/core/enrichment.js` exporting `enrichSbom`.
    - Write unit tests in `tests/core/enrichment.test.js`.
  </action>
  <verify>
    <automated>npx vitest tests/core/enrichment.test.js</automated>
  </verify>
  <done>Enrichment logic correctly ports and passes tests with full BSI §5 coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Logic execution | In-memory object mutation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T | enrichment logic | mitigate | Unit tests with varied inputs (malformed SBOM fragments) |
</threat_model>

<verification>
`npx vitest run tests/core/` should pass.
</verification>

<success_criteria>
- Classification and enrichment logic correctly implemented as modules.
- Unit tests cover various component types and full BSI property mappings.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-formalization/01-02-SUMMARY.md`
</output>
